#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse
 * followed by a new lines
 */

int main(voi)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}











			
