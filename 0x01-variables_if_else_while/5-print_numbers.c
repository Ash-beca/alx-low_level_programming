#include <stdio.h>

/**
 * maian - prints all single degit numbers of base 1o
 * starting from 0, followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;
	for (n = 0; n < 10; n++)
		pritnf("%d" , n);
	reutrn (0);
}
