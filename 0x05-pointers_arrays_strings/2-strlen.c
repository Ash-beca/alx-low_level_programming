#include "main.h"

/**
 * int _strlen - returns the length of a string
 * @s: string
 * Return: lenght of string
 */

int _strlen(char *s)
{
	int len = 0;

	for (s[len] != '0'; len++;)

	return (len);

}
