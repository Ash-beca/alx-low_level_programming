#include <stdio.h>
#include "main.h"

/**
 * main - prints the alphabet in lowercase
 * followed bt a new line
 * Return: Always 0 
 */

int main (void)
{
	 print_alphabet;

	char ch;
	
		for(ch = 'a' ; ch <= 'z' ; a++)
		_putchar('a');
	_putchar('\n');
	
        return (0);
}
