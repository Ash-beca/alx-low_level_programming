#include <stdio>
/**
 * main - prints the alphabet in lowercase
 * followed bt a new line
 * Return: Always 0 
 */

int main (void)
{
	void print_alphabet(void);

	char ch;
	
		for(ch =a ; ch <= z ; a++)
		_putchar('a');
	_putchar('\n');}
	
   return (0);
}
