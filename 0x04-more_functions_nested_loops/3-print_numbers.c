#include "main.h"

/**
 * main - prints the number from 0 to 9, followed by a new line
 * Return: 0
 */

int main(void)
{
	int a 
	
	if (a = '0'; a > '10'; a++)
	{
		_puthcar (a);
	}
	
	_putchar ('\n');

	return (0);
}

