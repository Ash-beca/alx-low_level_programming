#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 *@c: checks for digits
 *
 * Return: 1 if c is a digit, otherwise 0
 */

int _isdigit(int c);
{
	char c;

	int i = o;

	if ( c = '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
