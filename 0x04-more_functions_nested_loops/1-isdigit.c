#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 *@c: parameter
 *
 * Return: 1
 */

int _isdigit(int c);
{
	char b;

	int i = 0;

	for (b = '0'; b <= '9'; c = c++)
	{
		if (b == c)
		{
			i = 1;
		}
	}
	return (i);
}
